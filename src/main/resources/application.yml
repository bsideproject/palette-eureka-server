server:
  port: 8761

spring:
  application:
    name: eureka-server
  main:
    banner-mode: off
  profiles:
    default: dev
    group:
      local: local
      dev: dev

eureka:
  client:
    register-with-eureka: false # ?? ??? ?????? ???? ??? ?? ??
    fetch-registry: false # ??????? eureka ???? eureka ????? ??? ???? ??? ??

# ===============================
# = logback
# ===============================
logging:
  config: classpath:logback-spring.xml
